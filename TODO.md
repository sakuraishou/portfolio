# TODO（あとでやる）

> 直近のリデザインは完了し `page-add` に push 済み：
> MV/About/Skills/Works/詳細/manual のスペックシート化、フォント3書体化（Zen Kaku / Fraunces / IBM Plex Mono、Dancing・Quicksand 廃止）、`lg`→`md` 統一、Works 詳細 `/works/[id]`（ケーススタディ＋PREV/NEXT）、端末ショーケース共通化、社外秘トグル、404 刷新、ドキュメント更新。
> 残りは下記（ほぼ **コンテンツ入力** と任意項目）。

## 1. Works に実案件を登録する（最優先・コンテンツ入力）

仕組み（カード一覧・`/works/[id]` ケーススタディ・端末ショーケース・社外秘トグル）は実装済み。あとは **管理画面（/admin → Projects）に案件を登録**するだけ。

- 文面ドラフトは [docs/works-draft.md](docs/works-draft.md) に4件分（① 整骨院 予約＋CRM ② コールセンター道案内 ③ 週報自動生成 ④ WordPress掲載管理）完成済み。各項目をコピペ入力する。
- **社外秘の社内アプリ**：登録して「画像を公開しない（社外秘）」にチェック → 画像なしでも「社外秘のため画像は非公開」表示になる。
- 担当範囲は先に「Project Scopes」に選択肢を登録（企画・要件定義 / UI設計 / フロントエンド / バックエンド / インフラ・DB設計）。
- ケーススタディ4項目（課題/技術選定/工夫/結果）が1つでも入れば、カードから「詳細を見る」導線が出る。

### works-draft.md の未確定（【要記入】/【要確認】）を埋める
- 各案件の **制作開始月**（yyyy.MM）
- ① モノレポの具体ツール名（pnpm workspace / Turborepo / Nx）、LINE 連携の実装手段
- ②③④ の実績名（汎用名でOKか）、TypeScript 使用有無

## 2. 公開後：Works に実スクリーンショットを追加（任意）

- 現状は「画像なし方針」（端末枠は `NO IMAGE` 表示）。公開できる案件は **PC 用 横長 1440×686 / SP 用 縦長 540×960** をアップロードすると端末ショーケースに表示される。
- 整骨院CRM は VPS 公開後に **サイトURL** も入れると詳細ページに「サイトを見る」ボタンが出る。

## 3. About の文言を CMS 管理化（任意）

- 現状 About の本文・スペック表・リードは `index.tsx` にベタ書き。管理画面から編集したくなったら `about`（global など）を作る＝システム改修。

## 4. 仕上げ確認

- About のプロフィール写真（4:5・`object-fit: cover`）と manual 表紙の人物写真の顔の切れ方を実機で確認し、必要なら `object-position` を調整。
- 各セクションの文字サイズ最終バランス（PC 本文 `1.6rem` 基準）を実機で再確認。
- Skills は SP 2列 → `md` で 4列。タブレットで詰まって見えないか確認（必要なら列数だけ調整）。

## 5. main へのマージ

- 一連の作業は `page-add` ブランチ。確認できたら `main` へプルリク／マージ。
